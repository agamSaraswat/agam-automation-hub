export type PageKey = "dashboard" | "jobs" | "linkedin" | "gmail" | "settings" | "runs";

export const PAGE_LABELS: Record<PageKey, string> = {
  dashboard: "Dashboard",
  jobs: "Jobs",
  linkedin: "LinkedIn",
  gmail: "Gmail",
  settings: "Settings",
  runs: "Runs / Logs",
};
